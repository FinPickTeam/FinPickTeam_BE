<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.alarm.mapper.AlarmMapper">

    <insert id="insert">
        INSERT INTO alarms(user_id, message) VALUES (#{userId}, #{message})
    </insert>

    <insert id="insertAll">
        INSERT INTO alarms(user_id, message) SELECT id, #{message} FROM user
    </insert>

    <select id="getAlarms" resultType="org.scoula.alarm.domain.AlarmVO">
        SELECT * FROM alarms WHERE user_id=#{userId} ORDER BY created_at DESC
    </select>

    <update id="update">
        update alarms set is_read=true where id=#{id}
    </update>

    <update id="updateAll">
        update alarms set is_read=true where user_id=#{userId}
    </update>

</mapper>