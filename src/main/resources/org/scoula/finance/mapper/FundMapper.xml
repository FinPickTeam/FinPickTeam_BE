<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.finance.mapper.FundMapper">

    <select id="getFundList" resultType="org.scoula.finance.dto.fund.FundListDto">
        SELECT
            *
        FROM fund_list
    </select>

    <select id="getFundListByFundProductId" resultType="org.scoula.finance.dto.fund.FundListDto">
        SELECT
        *
        FROM fund_list
        WHERE id IN
        <foreach collection="list" item="productId" open="(" separator="," close=")">
            #{productId}
        </foreach>
    </select>

    <select id="getFundDetail" resultType="org.scoula.finance.dto.fund.FundDetailDto">
        SELECT
            *
        FROM fund_list
        WHERE id = #{productId}
    </select>

    <select id="getFundRecommendationList" resultType="org.scoula.finance.dto.fund.FundRecommendationDto">
        SELECT
            *
        FROM fund_list
    </select>
</mapper>